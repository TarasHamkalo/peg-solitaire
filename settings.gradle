rootProject.name = 'gamestudio-7800'

[
    'client',
    'game-server',
    'resource-server:app',
    'resource-server:service-rest-api',
    'oauth-server',
    'data',
    'core'
].forEach {
    include it
}